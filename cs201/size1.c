#include <stdio.h>
#include <stdlib.h>

int main() {

    printf("the size on an unsigned long is %lu\n", sizeof(int));
    printf("the size of a char is %lu\n", sizeof(char));
    printf("the size of a float if %lu\n", sizeof(float));
    printf("the size of a double is %lu\n", sizeof(double));
    printf("the size of a long int is: %lu\n", sizeof(long int));

    return 0;
}